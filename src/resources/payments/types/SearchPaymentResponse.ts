/**
 * This file auto-generated by Fern from our API Definition.
 */

import { PrimerApi } from "../../..";

export interface SearchPaymentResponse {
  /** You can use this ID to retrieve the payment details or perform downstream operations. */
  id: PrimerApi.payments.PaymentId;
  /** The date and time at which the payment was created in UTC format. */
  date: Date;
  /** As the payments are created, processed, and finalised, they go through a number of states that you will get as an API response, through webhook notifications, and in the Dashboard. These states are used across all processors, as processor specific states are mapped to these. An additional message, in the field processor may also be included that details the reason for the state, primarily on failure states. */
  status: PrimerApi.payments.PaymentStatus;
  /** Your reference for the payment. */
  orderId: PrimerApi.commons.OrderId;
  /** The payment processor used for this payment. */
  processor?: PrimerApi.payments.Processor;
  /** The 3-letter currency code in ISO 4217 format. e.g. use USD for US dollars. */
  currencyCode: PrimerApi.commons.CurrencyCode;
  /** The payment amount, in minor units. e.g. $7 would show as 700. */
  amount: number;
  /**
   * Additional data to be used throughout the payment lifecycle.
   * A dictionary of key-value pairs where the values can only be strings or integers.
   * e.g. {"productId": 1001, "merchantId": "a13bsd62s"}
   *
   */
  metadata?: Record<string, unknown>;
}
